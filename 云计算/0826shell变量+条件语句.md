---
date: "20250826"
docx:
  - shell脚本变量.docx
  - shell脚本条件判断.docx
---
# shell脚本变量
## 笔记

==shell脚本介绍==

>	定义
	应用场景（重复、批量、自动化、状态监控、定时、运维）
	
==shell脚本规范==
>脚本格式
	\#！/bin/bash
	\#是注释
	变量、函数
	顺序语句
执行脚本三种方式
	指定解释器运行，不需要x权限
	. /a.sh，需要x权限
	source a.sh 或 .a.sh不需要x权限,当前shell执行
脚本后缀名

 ==sehll变量==
 
>自定义变量

 ==特殊符号==
 
 >双引号
	 被解析
 单引号
	 不被解析
 反引号
	 等于${  }
	 
==read命令==

>read -p ‘提示’

==变量生效范围==
>export命令
>分类
	普通变量/本地变量/当前shell
	环境变量/当前shell+子shell
	局部变量/函数内定义
	特殊变量/固定

==数值变量运算==

>1.expr
	expr $x + - \\* / % $y
   2.$\[1+1\]

==系统环境变量==

>1.env 
>2.set declare 
>3.export 
unset命令

==位置变量==

>$0 1 2 ..9 {10}  

==预定义变量==

>$0 # * @ ?
成功0 失败非o
>|表达式|含义|参数处理方式|
|`$*`|所有参数当成一个整体|把所有参数合成 **一个字符串**|
|`$@`|所有参数分别展开|把每个参数当作 **独立的字符串**|

# shell脚本条件判断

==程序执行方式==

>顺序 选择 循环

==条件测试==

>命令测试
	1.test 表达式
	2.\[表达式\]
>test 3 -eq 3 && echo yes   # 输出 yes
   [ 3 -eq 3 ] && echo yes    # 同样输出 yes

==文件测试==

==数值比较==

==字符串比较==

>字符串比较、逻辑测试&&、||、！
 
 ==if条件语句==

>单分支
	if \[条件]
	then
		命令
	fi
双分支
	if \[条件]
	then
		命令
	else
		命令
	fi
多分支
	if \[条件]
	then
		命令
	elif    \[条件]
	then
		命令
	else
		命令
	fi


