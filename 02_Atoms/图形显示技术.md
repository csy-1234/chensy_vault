**适配屏幕宽度、像素密度与触控交互**，技术上依赖 **viewport、响应式布局、相对单位、高分屏图片、触控优化**。

物理像素Physical Pixels
设备屏幕真正的点阵；硬件层面概念；CSS 并不会直接用它布局；

CSS 像素 (CSS Pixels)
浏览器用来做网页布局的单位；CSS 像素是虚拟的，用来统一网页布局的，而不是物理像素

屏幕宽度 /物理分辨率        单位px像素

视口viewpoint