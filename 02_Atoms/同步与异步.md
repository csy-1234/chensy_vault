同步异步描述的
	是否顺序执行
	是否阻塞

阻塞和回调是同步异步的行为描述

同步=阻塞
异步不阻塞
异步后可选回调callback、prommise、其他方式异步和处理


异步与线程
异步和线程没有必然的关系
一个线程也可以异步
异步操作的本质是：它让任务在后台执行，**不阻塞主线程**，而主线程可以继续处理其他任务。这个 "后台执行" 的任务不一定要通过多线程来实现。
实际上，**事件循环**（Event Loop）机制和 **回调函数**，使得异步操作在单线程环境下也能有效运行。
